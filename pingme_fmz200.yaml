name: PingMe签到
description: 😀 新增PingMe签到 | 🤫 支持获取签到参数和每日签到&任务 | 🤫 原作者 @ZenMoFiShi | mickeu修改为支持 🎈Egern | 😀 仅测试了Egern
compat_arguments:
  PROXY: PROXY
compat_arguments_schema:
  PROXY:
    name: PROXY
    description: 代理规则使用的策略
    default_value: PROXY
    options:
    - PROXY
    - DIRECT
    - 手动选择
    - 自动选择
    - 故障转移
    - 负载均衡
author: mickeu 修改为Egern版
icon: https://raw.githubusercontent.com/axhani/icon/refs/heads/main/pingme&wetalk.png
dns: {}
rules:
- domain:
    match: api.pingmeapp.net
    policy: PROXY
scriptings:
- schedule:
    name: PingMe签到
    cron: 30 8,20 * * *
    script_url: https://raw.githubusercontent.com/fmz200/wool_scripts/main/Scripts/PingMe/PingMeSignin.js
    timeout: 300
- http_request:
    name: PingMe获取签到参数
    match: ^https:\/\/api\.pingmeapp\.net\/app\/queryBalanceAndBonus
    script_url: https://raw.githubusercontent.com/fmz200/wool_scripts/main/Scripts/cookie/get_cookie.js
    timeout: 60
mitm:
  hostnames:
    includes:
    - api.pingmeapp.net